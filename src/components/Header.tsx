import styled from 'styled-components';

const Heading = styled.h2`
  margin: 50px auto;
`;

const Header = () => {
  return <Heading>Scroll down to get more random cats 🐱</Heading>;
};

export default Header;
